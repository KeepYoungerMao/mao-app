server:
  port: 9090

spring:
  application:
    name: mao-app
  r2dbc:
    url: r2dbc:mysql://localhost:3306/mao-mms
    username: root
    password: root

app:
  jwt:
    private-key: classpath:private-key.pem
    public-key: classpath:public-key.pem
    access-token-expiration: 3600000
    refresh-token-expiration: 604800000